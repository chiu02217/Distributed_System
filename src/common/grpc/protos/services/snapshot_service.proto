syntax = "proto3";

import "snapshot_message.proto";

service SnapshotService {
    rpc TriggerSnapshot(TriggerSnapshotRequest) returns (TriggerSnapshotResponse);
    rpc RegisterWorkerId(RegisterWorkerIdRequest) returns (RegisterWorkerIdResponse);
}